# SITAS 联调问题记录（非 bug，待组长A 统一决定）

> **用途**：记录联调阶段发现的**非 bug 类问题 / 技术取舍 / 待决事项**。
> bug 直接修复，不记在这里；本文件只记「需要组长 DavidChristof（成员A）统一审阅决定是否处理」的事项。
> 成员B 在联调阶段维护本文件，后续发现非 bug 问题请追加到下方表格。

---

## 待决事项清单

| # | 日期 | 事项 | 说明 | 建议 |
|---|---|---|---|---|
| 1 | 2026-08-19 | 简历解析降级时姓名为空 | 成员A 的 `resume_parser` 规则模式只提取邮箱/电话，不提取姓名；姓名依赖 LLM 或前端表单 `name` 字段。前端上传时会传 `name`，故真实流程不受影响。 | 如需无 key 也能解析姓名，可在规则提取里加「首行/姓名」启发式 |
| 2 | 2026-08-19 | 报告未转发 `hire_recommendation`（录用建议） | 成员B 的 `_adapt_report` 未转发该字段（因前端契约未使用），但服务层 `report.generate_report` 实际会返回。 | 若前端后续要展示「录用建议」，在 `_adapt_report` 补一行即可 |
| 3 | 2026-08-19 | `build_index` 不幂等 | 成员A 的 `retriever.add_documents` 用 `extend` 累积，`build_index` 重复调用会重复文档。当前仅启动时调用一次，暂无影响；将来实现 `/api/knowledge/build` 时需注意。 | 建议 `build_index` 先清空 retriever 文档再重建 |
